import { createStore } from 'vuex'

export default createStore({
  state: {
    API_URL: process.env.VUE_APP_ROOT_API
  },
  /* getters:{
    getApiUrl: state
  }, */
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
